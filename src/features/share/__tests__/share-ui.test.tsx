import { render, screen, waitFor } from '@testing-library/react'; import userEvent from '@testing-library/user-event'; import { describe, expect, it, vi } from 'vitest'; import { ShareSheet } from '../components/ShareSheet';
describe('share sheet',()=>{it('defaults to coarse location',async()=>{const create=vi.fn().mockResolvedValue({url:'/share/demo'});render(<ShareSheet tripId="trip-demo" createSession={create}/>);await userEvent.click(screen.getByRole('button',{name:'生成家属链接'}));await waitFor(()=>expect(create).toHaveBeenCalled());expect(create.mock.calls[0][0].scopes).not.toContain('precise_location');});});
